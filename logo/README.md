# logo 迭代记录

**当前在用的是仓库根目录的 `logo.png` —— 也就是下表的第 08 版。**
这里按**作者给出的顺序**留档了全部 8 次迭代（编号跟作者原始文件的 `1`–`8` 一致），
想换回哪一版直接盖过去就行：

```bash
cp logo/01-boar-emblem.png logo.png
```

| # | 预览 | 说明 |
| --- | --- | --- |
| 01 | ![01](01-boar-emblem.png) | 扁平徽标：深蓝底 + 金色野猪 + 斜插战锤 + 肩上齿轮。只有三四个纯色，缩到 60px 也认得出来 |
| 02 | ![02](02-anime-girl.png) | 二次元：粉发骑士少女 + 木战锤 + 一只戴小铁盔的野猪崽。画风参考「三月七」那张 app 图标，角色是原创的 |
| 03 | ![03](03-boar-chibi.png) | 二次元：小野猪骑士 —— 粉肤色圆脸、大眼睛、小獠牙、金棕鬃毛，戴皇冠、扛小木锤 |
| 04 | ![04](04-mecha-illust.png) | 精绘全身：银发兽耳少女 + 齿轮与机械框架，细节最多的一版（石墙背景） |
| 05 | ![05](05-chibi-tablet.png) | Q 版全身：少女坐在皇室战争棋盘上举着平板看战局，周围散落金币与圣水 |
| 06 | ![06](06-fullbody-arena.png) | Q 版全身（第 2 稿）：同构图，人物更满、头上多了齿轮金冠 |
| 07 | ![07](07-face-dark-bg.png) | 头像特写 · **深色底** |
| 08 | ![08](08-face-white-bg.png) | 头像特写 · **白底** ← **当前在用**（与仓库根的 `logo.png` 相同） |

> 07 / 08 是同一张图的两个底色版本，只有背景不同。
> 归档里的 04 / 05 / 06 原本是 1024×956，统一处理成 512×512 便于对照。

## 换 logo 只动一处

只改仓库根的 `logo.png` 就够了：

- `README.md` 头部引用的是 `./logo.png`；
- 出发布包时要把它一起放进包根（不然包里的 README 会挂一张碎图）。
  > 以前这是 `tools/install.py` 干的，那个脚本已随「去 Python」删除 ——
  > 现在出包是手工组装，这一步要**自己记得**。

## 约定

- **512×512 PNG**、正方构图，README 里按 `width="200"` 显示。
- **背景色会影响深色主题下的观感**：白底图（第 08 版）在 GitHub 深色主题下会显示成一块白方块；
  深色底的 07 在浅色主题下反过来。想两全其美只能用**透明底**，目前还没有这一版。
  真要抠的话，扁平线稿请用「从边界洪泛填充近似背景色」的办法，**别用亮度阈值** ——
  那样会把发色、肤色这类浅色主体也抠成半透明。
- **带水印的图不许入库。** AI 生成的图右下角会打一行半透明白水印，入库前必须抹掉；
  做法与踩过的坑记在用户级参考文档里（`reference/tools-and-services.md`）。
  判断有没有抹干净要**放大目视**，别信自制的「亮度异常」指标 —— 平滑渐变会把它骗过去。
- **源图不一定是方图，也不一定是 PNG。** 统一按这个流程过一道：
  1. 非 PNG 先用 `ffmpeg -i 源图 中转.png` 转一道。
     ⚠️ 本机没装 ImageMagick；Windows 自带的 `convert.exe` 是 **FAT→NTFS 转换工具**，
     不是图像命令，千万别当 ImageMagick 用。
  2. 量出「非白内容」的包围盒：写个十几行的纯标准库脚本按 PNG 行/列扫一遍即可
     （**这种临时工具不进仓库**）。**它要按「每行/列至少若干个像素」才算「这一行有内容」**——
     否则边缘一个 JPEG 噪点就会把包围盒撑满整张图（实测踩过）。
  3. 裁/缩：近方图直接 `ffmpeg -vf "crop=边长:边长:x:y,scale=512:512:flags=lanczos"`。
     纯标准库那套只支持整数倍降采样，任意尺寸交给 ffmpeg 更省事。
  4. 想一次看多张，用 ffmpeg 的 `hstack` / `vstack` 拼成一张总览图 ——
     比一张张翻快得多（本文件上面那张对照表就是这么核对的）。
- 再加新版本就放 `09-xxx.png` 并补一行表格，**不要覆盖已有文件** ——
  这一目录的用途就是「留着还能换回去」。
