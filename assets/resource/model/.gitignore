# OCR 模型放这里（det.onnx / rec.onnx / keys.txt）。
#
# 2026-09-23：**用上了**，所以改成提交进来。
# 之前这里是空的（`*` 全忽略），因为项目一个 OCR 节点都没用 —— 圣水、塔血、皇冠数
# 都是「宽度就是数值」加色块计数读出来的。现在文字/弹窗识别要走框架的 `OCR`，
# 而 `recognition: "OCR"` 的节点会去这个目录加载模型，缺了识别直接失败。
#
# 用的是 PP-OCRv5 mobile（det + rec + keys）。官方预转换版：
#   https://github.com/MaaXYZ/MaaCommonAssets/tree/main/OCR
# 体积约 21 MB，**必须提交** —— 发布包和 CI 都靠它，只留在本地的话下一个包就没了。
*
!.gitignore
!ocr/
!ocr/det.onnx
!ocr/rec.onnx
!ocr/keys.txt
