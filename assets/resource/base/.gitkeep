这个目录必须存在，哪怕是空的。

原因：MFAAvalonia 跑 pretask 时，把「工作目录」硬编码成了
「<数据目录>/resource/base」（源码 v2.16.1：MaaProcessor.ResourceBase
= Path.Combine(DataRoot/resource, "base")），而 ProcessStartInfo 的
WorkingDirectory 指向一个不存在的目录时，Process.Start 会直接抛
「目录名无效」—— 整个 pretask 连启动都启动不了。

所以这里的空目录不是垃圾，它是「环境准备」那道安全闸门能跑起来的前提。
tools/validate.py 会检查它在不在。

（另外注意：pretask.exec 也是相对这个目录解析的，且不会去 PATH 里找，
细节见 assets/interface.json 里 pretask 那段注释。）
